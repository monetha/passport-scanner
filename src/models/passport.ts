export interface IPassportRef {

  /**
   * Block number in hex
   */
  blockNumber: string;

  /**
   * Block hash
   */
  blockHash: string;

  /**
   * Passport address
   */
  passportAddress: string;

  /**
   * Passport owner address
   */
  ownerAddress: string;
}
