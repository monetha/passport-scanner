export interface IPassportRef {

  /**
   * Block number in hex
   */
  blockNumber: string;

  /**
   * TX hash
   */
  txHash: string;

  /**
   * Passport address
   */
  passportAddress: string;

  /**
   * Passport owner address
   */
  ownerAddress: string;
}
